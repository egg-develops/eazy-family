diff --git a/node_modules/@capacitor/cli/dist/ios/update.js b/node_modules/@capacitor/cli/dist/ios/update.js
index cbd21ec..0f694e7 100644
--- a/node_modules/@capacitor/cli/dist/ios/update.js
+++ b/node_modules/@capacitor/cli/dist/ios/update.js
@@ -60,7 +60,8 @@ async function generateCordovaPackageFiles(cordovaPlugins, config) {
 }
 async function generateCordovaPackageFile(p, config) {
     const iosPlatformVersion = await (0, common_1.getCapacitorPackageVersion)(config, config.ios.name);
-    const iosVersion = (0, common_2.getMajoriOSVersion)(config);
+    // capacitor-ios-spm-fix: clamp to .v17 (max valid for swift-tools 5.9); see util/spm.js.
+    const iosVersion = Math.min(parseInt((0, common_2.getMajoriOSVersion)(config), 10) || 14, 17);
     const headerFiles = (0, plugin_1.getPlatformElement)(p, platform, 'header-file');
     let headersText = '';
     if (headerFiles.length > 0) {
diff --git a/node_modules/@capacitor/cli/dist/util/spm.js b/node_modules/@capacitor/cli/dist/util/spm.js
index 3ae0fcc..a48236b 100644
--- a/node_modules/@capacitor/cli/dist/util/spm.js
+++ b/node_modules/@capacitor/cli/dist/util/spm.js
@@ -81,7 +81,11 @@ exports.removeCocoapodsFiles = removeCocoapodsFiles;
 async function generatePackageText(config, plugins) {
     var _a, _b, _c;
     const iosPlatformVersion = await (0, common_1.getCapacitorPackageVersion)(config, config.ios.name);
-    const iosVersion = (0, common_2.getMajoriOSVersion)(config);
+    // capacitor-ios-spm-fix: swift-tools-version 5.9 only knows .iOS up to .v17,
+    // but getMajoriOSVersion() returns the first IPHONEOS_DEPLOYMENT_TARGET in the
+    // pbxproj — which is the widget extension (18.5), producing an invalid
+    // `tools 5.9 + .v18` Package.swift. Clamp to 17 so the file always compiles.
+    const iosVersion = Math.min(parseInt((0, common_2.getMajoriOSVersion)(config), 10) || 14, 17);
     let packageSwiftText = `// swift-tools-version: 5.9
 import PackageDescription
 
