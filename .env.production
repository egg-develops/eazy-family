# Production build-time env for Vite (committed).
# Only PUBLIC, client-safe values belong here — everything in a VITE_* var is
# inlined into the browser bundle. The PostHog project API key is a public
# client key (safe to expose); secrets stay in the Vercel dashboard, not here.
VITE_POSTHOG_KEY=phc_kdn42LCaXzJgBHGqPTjDsQLu4EqyjabKfChFipFzanYo
VITE_POSTHOG_HOST=https://eu.i.posthog.com
